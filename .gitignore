# dependencies
node_modules/

# runtime data — SQLite db, uploaded base image, tile refs, final mosaic.
# All of this is regenerated by the server; never commit it.
/data/

# SQLite sidecar files (WAL mode), in case the data layout changes
*.db
*.db-wal
*.db-shm

# logs / process manager
*.log
logs/
.pm2/

# env / secrets
.env
.env.local

# nerdy load-screen quotes — maintained on the server, not in git (see quotes.example.csv)
quotes.csv

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
